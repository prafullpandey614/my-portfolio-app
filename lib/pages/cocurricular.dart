import 'package:flutter/material.dart';

class Curricular extends StatelessWidget {
  const Curricular({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Co- Curricular Activities"),
      ),
    );
  }
}
